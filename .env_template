# Core Configuration
NODE_ENV=production
RUST_LOG=info,claude_flow=debug,tcp=debug
RUST_BACKTRACE=1

# TCP-based Claude Flow MCP Configuration
CLAUDE_FLOW_HOST=multi-agent-container
MCP_TCP_PORT=9500
MCP_TRANSPORT=tcp
MCP_RECONNECT_ATTEMPTS=3
MCP_RECONNECT_DELAY=1000
MCP_CONNECTION_TIMEOUT=30000

# RagFlow Configuration
RAGFLOW_API_BASE_URL=http://ragflow-server:9380
RAGFLOW_AGENT_ID=

# API Keys
OPENAI_API_KEY=
PERPLEXITY_API_KEY=
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=

# Optional: GPU / CUDA Configuration
# NO_GPU_COMPUTE=true               # Uncomment to disable GPU features entirely.

# NVIDIA_VISIBLE_DEVICES: Controls which GPU(s) are exposed to the Docker container.
# - To use a specific GPU, provide its UUID (find with `nvidia-smi -L`).
# - If you expose only ONE GPU, its index inside the container will be 0.
# NVIDIA_VISIBLE_DEVICES=GPU-553dc306-dab3-32e2-c69b-28175a6f4da6

# CUDA_DEVICE: Tells the application which GPU index to use *inside the container*.
# - If NVIDIA_VISIBLE_DEVICES exposes only one GPU, set this to 0.
# - If NVIDIA_VISIBLE_DEVICES is "all", set this to the desired physical GPU index (e.g., 1 for the Quadro).
CUDA_DEVICE=0

# CUDA_ARCH: Specifies the CUDA compute capability of the target GPU.
# - 86 for RTX 30-series (Ampere)
# - 89 for RTX 40-series / A6000 (Ada)
# CUDA_ARCH=86

# Optional: Nostr Configuration
NOSTR_PRIVATE_KEY=
NOSTR_RELAYS=wss://relay.damus.io,wss://relay.nostr.band