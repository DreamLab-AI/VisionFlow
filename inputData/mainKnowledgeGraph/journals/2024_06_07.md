- [ipiv's Morph - img2vid AnimateDiff LCM / Hyper-SD - v3.0 (Hyper-SD + v3 mm) | Stable Diffusion Workflows | Civitai](https://civitai.com/models/372584/ipivs-morph-img2vid-animatediff-lcm-hyper-sd)
- DOING Stash Julies stuff
  :LOGBOOK:
  CLOCK: [2024-06-07 Fri 19:24:39]
  :END:
- Tuesday Demo
	- Redo my RAG text
	- Automate selecting larger files and script removing the urls?
		- [jjohare/logseq-create-RAG: A docker image to publish logseq graphs to a single optimised text file for retrieval augmented generation (github.com)](https://github.com/jjohare/logseq-create-RAG)
		- ```text
		  import re
		  
		  def clean_text(text):
		      # Remove URLs starting with http or https
		      text = re.sub(r'http\S+', '', text)
		  
		      # Remove unwanted characters: - [ ] ( ) * { }
		      text = re.sub(r'[-\[\]()\*{}]', '', text)
		  
		      # Remove double square brackets [[...]]
		      text = re.sub(r'\[\[|\]\]', '', text)
		  
		      return text
		  
		  def main(input_file, output_file):
		      with open(input_file, 'r', encoding='utf-8') as infile:
		          content = infile.read()
		  
		      cleaned_content = clean_text(content)
		  
		      with open(output_file, 'w', encoding='utf-8') as outfile:
		          outfile.write(cleaned_content)
		  
		  if __name__ == "__main__":
		      main("Bitcoin.md", "file_cleaned.md")
		  
		  
		  ```
	-