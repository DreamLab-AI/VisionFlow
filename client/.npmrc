# Security: Block installation of compromised testing packages
# These packages have malware in their dependency chain
# DO NOT REMOVE THESE BLOCKS until the supply chain attack is resolved

# Block testing libraries with compromised dependencies
save-exact=true
package-lock=true

# Audit on install
audit-level=moderate

# Prevent specific packages from being installed
# Note: npm doesn't have a direct block feature, but we document here
# See SECURITY_ALERT.md for details