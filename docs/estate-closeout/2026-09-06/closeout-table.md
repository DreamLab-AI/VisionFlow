# Post-sprint sanity check, 2026-09-06: closeout table

**Scope.** Every repo that received sprint work between 2026-09-01 and 2026-09-06 (VisionFlow, VisionClaw, agentbox, nostr-rust-forum, solid-pod-rs, dreamlab-ai-website, loom, prose-sanitiser, diagram-ir, knowledgeGraph, dream-engine), their hosted CI runs, the pinned inheritance between them, the running deployment on this host, the public surfaces in a real browser, the diagrams-as-code snapshot in VisionClaw `docs/diagrams`, and the estate TODO registers.

**Method.** One coordinating session (Fable) with Opus fixers and Sonnet researchers sharing state through RuVector `project-state` (`estate-post-sprint-sanity-2026-09-06-*`). Every verdict below cites a file, commit, run or receipt; commit messages alone were not trusted. Evidence files in this directory: `website-browser-receipt.json` (11 surfaces, `screenshots/`), `pin-ledger.md`, `diagram-audit.md`, `todo-evidence.md`.

**Verdict key.** GREEN: verified passing. FIXED: was red at 08:00Z, fixed and verified green. LOCAL: fixed and committed here, push needs the operator (the session's push permission was refused by the auto-mode classifier). OPS: needs an operator action no agent can perform. OPEN: recorded, not fixed this pass.

## Table

| # | Area | Finding at 08:00Z | Evidence | Action taken | Status |
|---|---|---|---|---|---|
| 1 | nostr-rust-forum CI | Wave landing 486ec5a red: fmt (12 files), clippy `type_complexity` at `relay_do/receipts.rs:747` plus a hidden `needless_as_bytes` in core tests, rustdoc links through a private module | run 34019155715 | rustfmt; `FakeReceiptRow` alias; `tag.len()`; links repointed at public re-exports (3 latent sites also fixed) | FIXED, pushed 7b2a126, run 34020797239 all 9 jobs green |
| 2 | dreamlab-ai-website CI | All five workflows red at 000a40e: `kit-pin-guard.yml:17` used mutable `actions/checkout@v4`, rejected by the repo's own pin-parity gate; `dream-kit-pin-guard.sh` emitted a false `PIN-DRIFT-RECORD-VER` (its `=`-sigil parse rotted when the manifest moved to exact pins on 2026-09-05); `dream.config.json` carried a fourth copy of the same shell; ci.yml path filters never re-ran the gate that rejects tag pins | runs 34019271789 to 34019272009 | checkout SHA-pinned (`11d5960a`, v4.4.0); guard is now a thin wrapper over `scripts/pin-parity.mjs`; nightly evaluator points at `pin-parity-check.sh`; path filters widened; 10 new vitest cases | FIXED, pushed df1c655, CI + kit-pin-guard + Test-and-Lint green (34020984611/13/16) |
| 3 | dreamlab-ai.com deployment | Live build is from 2026-08-31 14:06 (six days stale). The 07:29 deploys died at the pre-deploy gate (`PINS=failure VITEST=failure`); the green df1c655 push did not re-trigger Pages or Workers deploys (path filters) | browser receipt `dl-01-home`; runs 34019271928, 34019272009 | Dispatch attempted, refused by the classifier | OPS: `gh workflow run deploy.yml` and `workers-deploy.yml` on main |
| 4 | solid-pod-rs CI | 40f160c red: `chacha20` yanked (cargo-deny + cargo-audit, red since 2026-08-31); every stable-toolchain test job red while beta passes; tarpaulin fails on `tests/rate_limit_lru.rs:159` | run 34019080158 | `cargo update -p chacha20` (0.10.2; 0.10.0 and 0.10.1 both yanked). The stable-only failure was rustdoc: `cargo doc` runs only on stable with `-D rustdoc::broken-intra-doc-links`; four links fixed (`wac/resolver.rs:240`, three in `auth/replay.rs` module docs, where a `///` on the `pub mod` declaration makes rustdoc resolve the merged fragment in the parent scope). LRU test made deterministic by promoting the clock-parameterised `check_at` to public and driving virtual time; new sliding-window test | FIXED, pushed 62376e9, run 34022143944 all 20 jobs green, tarpaulin 85.34%. Note: `deny.toml` ignore for RUSTSEC-2026-0258 (h2 0.3) has a 2026-09-20 review date |
| 5 | VisionClaw runtime | `visionclaw_container` unhealthy (failing streak 495), rust-backend exiting in 40 ms. Not the Wave 3 credentials (all present). Layer 1: `Dockerfile.unified:307` copied the wrapper but not `scripts/lib/build-inputs.sh` it sources (ADR-2008). Layer 2: `src/uri/mod.rs:662` `include_str!`s `agentbox/schema/federation-kinds.json`, which neither the builder stage nor the compose mounts supplied, so the crate could not compile; the container had been running a 2 September binary and the wrapper's `cargo clean` fallback had destroyed it | `/app/logs/rust-error.log`; commits 18a813e75, af8d6495a, 3a3ce0ea9 | COPY `scripts/lib/` and `agentbox/schema` in dev and rust-builder stages; read-only bind of `agentbox/schema`; `launch.sh` hotpatch paths corrected; relaunched via host `up dev` | FIXED, pushed; health `healthy`, streak 0, `/api/health` 200; CI green on 3a3ce0ea9 |
| 6 | VisionClaw in browser | Client at :3001 renders 13,165 nodes / 153,957 edges on Three.js r183, `/wss` upgrades 101, live streaming socket confirmed server-side | browser receipt entry 11, `screenshots/visionclaw.png` | none | GREEN with 3 warnings: 88,421 edges (57%) silently not rendered above the 65,536 `GlassEdges` ceiling; three 403s at boot (`GET /api/broker/inbox` x2, `POST /api/layout/mode`) from unauthenticated dev client calls; physics toggle off at boot leaves the force-directed layout as a bounded slab |
| 7 | VisionClaw Ontology Federation CI | Red on every run since 2025-12-31: second checkout of private cross-owner `jjohare/logseq` with the repo-scoped `GITHUB_TOKEN` (reported as 404) | runs 34022991433, 33999469463 | Workflow now uses `secrets.ONTOLOGY_SOURCE_TOKEN` with a preflight that names the missing credential (af8d6495a) | OPS: add fine-grained PAT (Contents: read on jjohare/logseq) as `ONTOLOGY_SOURCE_TOKEN`; the repo has no secrets or variables at all |
| 8 | VisionFlow closeout tree | 41 files from the 2026-09-05 closeout (ADR-2007, estate-review corpus, receipts, rewritten workflows, gate tests) uncommitted; `drift-counter.test.sh` case 6 failing because the allowlist pinned agentbox at 89301ec7c while the sibling landed 21 commits (counts unchanged: 126 skills, 12 tools) | `tests/gates/run-all.sh` | Re-pinned allowlist and workflow ref to eb7794b17; all four gate suites green (103 checks); harness audit 100% pairing | LOCAL: 3db4785 |
| 9 | www.visionflow.info | Live is behind: still says 116 skills in three places (truth 126), missing six social meta tags and the published-crates bullet; site otherwise clean, Lighthouse desktop 100/100/100/100, plain-language toggle and full-report PDF verified | browser receipt `vf-01`, `vf-02` | Cause is the unpushed VisionFlow commit, not the deploy | LOCAL: publishes on push |
| 10 | VisionFlow canon claims | README.md:252 and `website/static/index.html:352` said a merged PR fires `ConceptElevated` through the Whelk gate; the symbol does not exist in VisionClaw source at HEAD. The Whelk consistency gate itself is real (`elevation_actor.rs:107`, GOV-7). Kernel figure (82 in 9 `.cu`) and handler split (19 directive + 25 query) still hold; LOC now 6,159 not 5,854. ecosystem-map G2 still says `MeshTransport` and NIP-42 are scaffold/false; both now exist (`nostr-bbs-mesh/src/transport.rs:249`, `relay_do/nip42.rs`) | grep at HEAD | Claims corrected; G2 annotated with a dated re-verification | LOCAL (this commit) |
| 11 | VisionFlow mobile a11y | `button#m-reading` aria-label did not contain its visible "Tech"/"Plain" text (WCAG 2.5.3 Label in Name; unweighted, so Lighthouse still scored 100) | browser receipt Lighthouse mobile | Name now derives from visible text; explanation moved to `title` | LOCAL (this commit) |
| 12 | loom | No `.github/workflows` at all; `cargo fmt --check` failed on 19 files from c86f462; 278 tests pass, clippy and cargo-deny clean. Hosted CI is not currently possible: `ruvector-core` is an unversioned path dep on `../ruvector` checked out at branch `report/agentbox-field-2026-07`, which exists only on the jjohare fork | pin-ledger §3, §4 | rustfmt committed | LOCAL: loom ba9f6bf. OPEN: pin ruvector-core to a rev or vendor it, then add CI (register G-19) |
| 13 | Pinned inheritance | agentbox `lib/solid-pod-rs.nix` pins solid-pod-rs at v0.5.0-alpha.3 (58 commits, 5 releases behind alpha.8) while `services/nostr-pod-bridge` path-deps compile against the alpha.8 sibling: two snapshots in one image, no documented hold | pin-ledger §2, §3 | none (decision needed: bump rev + hash + lockfile) | OPEN (register G-20) |
| 14 | Pinned inheritance | Healthy clusters: VisionClaw submodule = agentbox HEAD; website `KIT_REF` a7544687 agrees at five sites with the held pin documented; drift-counter pin four-way agreement; nix prose-sanitiser 0.1.1 and diagram-ir 0.1.0 match crates.io | pin-ledger §1, §5, §6 | none | GREEN |
| 15 | Pinned inheritance | Mutable GitHub Actions refs: VisionClaw 0/42 SHA-pinned, VisionFlow 0/19, forum 0/27, prose-sanitiser 0/7, diagram-ir 0/7, knowledgeGraph 0/3, dream-engine 0/16, agentbox 5/60; only dreamlab-ai-website (57/57) and solid-pod-rs (33/33) enforce pins. `docker-compose.solid-pods.yml:26` pulls `cloudflared:latest` with no digest while the unified compose pins the same image by digest; `LOOM_IMAGE` floats | pin-ledger §4, §7 | none | OPEN (register G-22) |
| 16 | Loom identity | `project/CLAUDE.md` and `agentbox.toml:1707` name the model "Qwen3.8-27B"; loom's own `docs/QWEN3.8-CONNECTION.md` addendum (2026-08-18) says the served variant is the Heretic-abliterated Q8_0 build on the same port. Façade healthy: 8,146 classes, 282,492 triples, backend reachable | pin-ledger §7; `curl :8084/health` | none (trust-boundary doc, operator's call) | OPEN (register G-18) |
| 17 | dream-engine | Dependabot PRs #1 to #6 open since 2026-08-16; local `dream-machine` main is one commit ahead of origin (2026-09-01 ledger row) | `gh pr list`; `git status -sb` | none | OPS: merge or close the six PRs; push dream-machine |
| 18 | Diagrams-as-code | 71 files anchored at bed6b617d, HEAD moved through Wave 3 to 3a3ce0ea9. Audit of 6,551 `path:line` citations: 10 files STALE with confirmed broken citations (VC-09, VC-23, VC-24, VC-26, VC-30, VC-32, AB-04, AB-13, AB-17, ES-08), 60 touched but intact, 1 clean. VC-26/30/32 describe the `solidWebSocket.ts` sub-protocol ADR-2100 deleted. ES-09.11 ci.yml job lines were wrong even at the stated anchor. The checker validates paths and Mermaid syntax but never citation content | `diagram-audit.md` | 74 files rewritten and re-anchored at 7a20db228 (every citation re-read at that tree, not the parent): VC-32 redrawn around `podNotificationManager`, VC-30 corrected to three sockets and `REGISTRY_NAME` "solid-pod", VC-26 body re-cited, AB-04/AB-11/ES-08 fail-open notes resolved (`requireSigned`), AB-13/AB-17 derived kind map with `federation-kinds.json` as a node, VC-24/VC-09/VC-23 relocated to real symbols; ES-09.11 rebuilt with the `dev-auth-release-gate` job, ES-09.13 re-derived after af8d6495a plus the omitted `pr-preview` job; ES-09.3/09.4, AB-02.3, AB-03.5, AB-23.6 found citing lines past end of file (pre-existing). 951 bulk relocations machine-proved by byte identity at old and new line. `--cite-check` added to `scripts/diagram-index-gen.js` (56 lines; found 18 past-EOF citations, now 0; 336 advisory range-end warnings unexamined) | FIXED, pushed VisionClaw 962dd558d, c642a21f8, 2e1b7cb34; checker 841/841 with 0 symbol warnings. A parallel read-only audit (session diagrams-d7) measured 94% of drift removed and then supplied 30 residual rows; 2 were real (VC-33.6 RBAC citations) and 28 were refuted by reading the landing lines. Lesson recorded in the README: a diff shift says where a line moved, not whether the citation was right before it moved; `--cite-check` now requires a function-named participant to cite inside that function's body |
| 19 | TODO registers | `docs/TODO-unified.md` said 40 open (undercounted by 2); five rows resolved by the sprint (M-1, M-6, G-3, G-9, G-13); DR-1/DR-2 premise wrong (cited commit does not touch the bench evaluator, `/dream` never re-run, no ADR-0057 exists); G-14 worse (97 of 98 VisionClaw ADRs `supersedes: []` after ADR-2043 to 2105 were minted); recall-gate contradiction resolved by a fresh run (PASS 189/200, 115/120); G-1 dev-auth guard is real at `src/main.rs:118` and release-gated by ADR-2086, only ADR-2037's status fields lie | `todo-evidence.md`; recall receipt `agentbox/backups/ruvector-sidecar/recall-runs/2026-09-06T08-55-47-514Z.json` | Registers rewritten: 5 resolved with evidence, ~20 rows corrected, 9 new rows (E-5, G-16 to G-22, DOC-5), count 46; agentbox backlog and VisionFlow roadmap rewritten | LOCAL: agentbox d31ad3fb, VisionClaw 7a20db228, VisionFlow bf2aa72 |
| 20 | New defects surfaced | GPU LOF kernel disagrees with the CPU oracle (max delta 0.702 vs 1e-3, `adr_2061_lof_matches_oracle` failing, `docs/GPU-wire-abi.md:176`); `services/secret-backup` crate built but not wired into `flake.nix`; `build-inputs.sh` does not count `include_str!` assets as build inputs (exactly the hole that caused row 5) and ADR-2008's claimed fixture tests do not exist; dreamlab-ai.com delivers CSP `frame-ancestors` and X-Frame-Options via `<meta>`, where both are ignored; deep routes 404 to non-JS clients by SPA design; mobile a11y 90 (contrast, missing `<main>`, touch targets) | fixer and browser reports | none | OPEN (G-16, G-17, DOC-5, and website follow-ups) |
| 22 | WasmVOWL | Escalated in the evidence pack, not previously in this table: the explorer has failing frontend state initialisation and a schema mismatch despite passing native Rust tests (closeout gate CP-02), and `/graph` requests neither the WASM nor the binary tier. Crate survey: `webvowl-wasm` is defined twice in the estate (WasmVOWL/rust-wasm 1.0.0 and knowledgeGraph/explorer/rust-wasm 0.3.4) with a byte-identical core and a diverged, larger knowledgeGraph copy whose `repository` field points at the upstream WebVOWL project; the name is unclaimed on crates.io and only one can take it | `todo-evidence.md` §5, §6; `crate-survey.md` | none | OPEN: merge into the knowledgeGraph tree per the survey plan, feature-gate the NGG1 binary path, fix metadata, then publish |
| 21 | Other substrates | agentbox main eb7794b17: CI, Security Invariants, Secret Scan green (Nix flake check red only on the dream branch, PR #4 two commits behind main). prose-sanitiser e6d456d CI green, 0.1.1 on crates.io. diagram-ir 16e3254 CI green, 0.1.0 on crates.io. knowledgeGraph 7bbf0aa build-and-verify green; narrativegoldmine.com renders 40 nodes on WebGL2. agentbox surfaces :8080 (code-server) and :9096 (NIP-98 door) refuse unauthenticated access correctly | `gh run list`; browser receipt | none | GREEN |

## Pushed this pass

| Repo | Commit(s) | CI |
|---|---|---|
| nostr-rust-forum | 7b2a126 | green (9/9) |
| dreamlab-ai-website | df1c655 | green (3 triggered workflows) |
| VisionClaw | 18a813e75, af8d6495a, 3a3ce0ea9, 7a20db228, 962dd558d | CI green; Ontology Federation red pending secret |
| solid-pod-rs | 62376e9 | green (20/20) |

## Local commits awaiting the operator's push

| Repo | Commit | Content |
|---|---|---|
| VisionFlow | 3db4785 | 2026-09-05 closeout tree, drift-counter re-pin |
| VisionFlow | bf2aa72 | roadmap rewrite |
| VisionFlow | (this commit) | canon corrections, a11y fix, this table and evidence |
| VisionClaw | (after agentbox push) | bump the agentbox submodule pointer to d31ad3fb; deliberately not committed while that agentbox commit is unpushed, since CI checkout would fail on the gitlink |
| agentbox | d31ad3fb | backlog update |
| loom | ba9f6bf | rustfmt |
| dream-machine | 7c30573 | 2026-09-01 ledger row (pre-existing) |

## Operator actions

1. Push the local commits above (VisionFlow x3, agentbox, loom, dream-machine), then bump and push the VisionClaw submodule pointer. VisionFlow's push runs the rewritten deploy, drift-counter, harness-fitness and fixture-drift workflows on hosted CI for the first time; fixture-drift is expected red until sibling credentials exist.
2. Dispatch `deploy.yml` and `workers-deploy.yml` in dreamlab-ai-website to publish the six-days-stale site.
3. Add `ONTOLOGY_SOURCE_TOKEN` (fine-grained PAT, Contents: read on jjohare/logseq) to DreamLab-AI/VisionClaw.
4. Decide the agentbox solid-pod-rs nix pin (alpha.3 held or bump to alpha.8) and the Loom model-identity doc.
5. Merge or close dream-engine dependabot PRs #1 to #6.

## Method notes for the next mesh

- The browser sidecar's `take_screenshot` with `filePath` refuses every path (MCP roots); raw CDP `Page.captureScreenshot` inside the container works. `lighthouse_audit` mobile leaves device emulation stuck on the tab; use a fresh tab afterwards. `list_network_requests` never reports WebSockets; verify upgrades with curl or server-side `ss`.
- `tmux send-keys -t 6` targets pane 6 of the active window, not the host shell; the host shell is `agentbox:6`. `./scripts/launch.sh rebuild dev` wipes the cargo volumes; `up dev` already rebuilds the image when the Dockerfile changed.
- `npx` fails with ENOSPC from the npm cache in this container; run `node node_modules/<pkg>/…` directly.
