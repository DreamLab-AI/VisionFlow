# Acceptance / hand-over notes (coordinator, 2026-09-22)
- WS-D must repoint VisionClaw `DECISIONS_DIR` from `mainKnowledgeGraph/pages/decisions` to the visionGraph layout (find where decision pages live now) or `rematerialise_decisions` finds nothing.
- Owner cut-over runbook: first local sync needs one-off `FORCE_FULL_SYNC=1` (pre-ADR-2114 stores lack corpus_source_identity); rebuild VisionClaw image after commit; then `ONTOLOGY_RETIREMENT_DEPLOYED=1` makes WS-F's 410 smoke binding.
- VisionClaw full test run at acceptance: two targets reported broken by in-flight cross-workstream edits — rec10_rec11_data_moat_test (NewAgentTrajectory.intent), ontology_agent_integration_test (confidence: Option<f32>). Identify owner and fix before hand-over.
- VisionFlow drift-counter gate red (skills 130 vs 127) — pre-existing; fix counts at hand-over.
- Builds in container need CPATH=/nix/store/…cuda-merged-12.9/include (pre-existing).
- 38 cited assets missing on disk (WS-I) — `vault validate` should report dangling asset links.
- Site size ~0.95 GB pre-migration; re-measure after WS-D; api/markdown dropped from site.
- SECURITY (2026-09-22 12:45): 13 live credentials in 6 working/ files (OpenAI, Perplexity, HuggingFace, bearer tokens); one on a public: true page. Redacted to $ENV form by WS-D (manifest in visionGraph/docs/migration-2026-09-22/credential-redaction.md); page un-flagged; vault validate gains SECRET_DETECTED, publish staging fails closed. OWNER MUST ROTATE all affected keys (values are in pushed git history since ≥2024-09). History rewrite: owner's call, recommended against.
- SECURITY UPDATE 12:55: PUBLIC exposure confirmed. DreamLab-AI/knowledgeGraph (public) gh-pages history: api/markdown/Project Automated Podcast.md @024324276 (2026-01-23) 2 OpenAI-key hits; api/markdown/perplexity.md + api/pages/perplexity.json 8 deploys 2025-11-16..2026-06-22, 1 Perplexity-key hit each. Current tip d119ac8b7 (2026-08-22) clean; both pages 404 live. OWNER RUNBOOK: (1) rotate OpenAI/Perplexity/HF/bearer; (2) delete+recreate gh-pages as an orphan from a fresh deploy (force push) and request GitHub Support cache purge for the old commits; (3) private repos: rotation suffices, no rewrite.
- PRE-PUSH REVIEW (owner): first deploy publishes 389 working pages (188 podcast-evidence + 193 flagged + 8 misc) and ~16 knowledge pages the old fence gate excluded — review docs/migration-2026-09-22/newly-published.md and un-flag as needed. First deploy also needs VAULT_GIT_REV set to the landed VisionClaw commit (or a vault release tag). Site 890 MB with 10 MB asset cap (9 files skipped); deploy fails >1 GB.
- Diff visionGraph/transcripts/.ingest-state.json against baseline at hand-over (a sub-fork wiped 960 entries; restored by ws-eg-followups).
- Identity rule to add to contracts/VAULT format: a working page's IRI derives from `title:`, not the filename (WS-D DUPLICATE_IRI finding).
- PUBLIC gh-pages history also contains 13 non-media assets that were published (grant application .docx, risk register .docx, IUK claim guidance .docx, company finance workbook .xlsm, a .zip, .owl/.json) — now withheld by the asset allowlist (Q17); strengthens the case for orphaning DreamLab-AI/knowledgeGraph gh-pages. Owner action.
- WS-J applied (git-reversible parts): knowledgeGraph ontology/pages + pipeline → archive/logseq-era-2026-09-22 (8,170 renames + README); visionGraph pipeline/ git rm (29 files; publishing-tools KEPT — explorer SPA); VisionClaw crates/vault-migrate git rm + workspace member removed. OWNER (classifier refused rm -rf): `rm -rf ~/workspace/vault ~/workspace/vault-working ~/workspace/logseq-publisher ~/workspace/logseq-publisher-rust ~/workspace/logseq-publisher-npm && rm ~/workspace/logseq` (+ remove the /mnt/mldata/githubs/logseq → project4 bind from agentbox compose).
- REASONER (16:05): whelk-rs (fork rev 79a1ee2) saturation is superlinear on this lattice (6,000 pages 287 s; 8,446 does not finish); cycles/transitivity/restrictions ablated — not the cause; equivalence (3,580 same-as) under test; flamegraph next. VisionClaw survives by classifying once and caching. Acceptance 2 (one class count) can be measured from `vault build --stats` page census independent of the inferred TTL; ontology-inferred.ttl is the one C3 artefact at risk until fixed. If unfixed at hand-over: ship asserted TTL + classification cache path, flag inferred TTL as the single open engineering item with its owner (whelk-rs fork).
- ACCEPTANCE partial run 16:45: ADR indexes valid in VisionFlow (13), VisionClaw (109), forum (13); agentbox reports ADR-2106 STALE by ledger design (verified_paths includes management-api/lib/ontology-propose.js, changed after verified_commit) → owner bumps verified_commit at the landing commit (intended per ADR-2058 discipline). Loom: 371 tests pass, golden-python untouched, stale_after in /health. Reasoner FIXED 16:39 (12 s full build).

## Landing notes (added 2026-09-22 20:22)

- **VisionClaw**: `main` advanced by the sidestr session to `1657efa85` (fast-forward a32abac57 + 1657efa85; both workflows green; ci.yml now `-p vault-core -p vault`). Overlap with our uncommitted tree: `crates/vault/src/nostr.rs` (rustfmt, ours already byte-identical), `crates/vault/src/propose.rs` (one rustfmt wrap at :395 inside a test we rewrote — take ours, run `cargo fmt`), `docs/adr/ADR-2116` (re-verification note + verified_commit bump only — keep theirs, re-verify after our changes). Land by: stash/commit ours on a branch, fetch, rebase onto origin/main, `cargo fmt`, full test run, then re-verify every ADR our change makes STALE.
- **agentbox**: `main` carries the sidestr session's `vault-cli` system-manifest catalogue entry (21882ed78) — keep it; our tree adds none. Security Invariants is red ONLY on `validate-adr-ledger` (16 stale + the 2106 collision from the rebuild-session commits) until our landing commit: ADR-2109 renumber + re-verify the stale records.
- **Owner-only steps unchanged**: rotate/confirm keys and orphan public gh-pages; set `VAULT_GIT_REV`; misc/ and Emergence rulings; untracked `rm -rf` deletions; image rebuilds for VisionClaw (FORCE_FULL_SYNC=1 on first local sync) and agentbox (VAULT_REPO now in agentbox.toml); Loom deploy + reload timer (acceptance 5).
- **agentbox flake.nix** (added 20:30): statix-clean in the working tree (11 findings from sidestr's list). statix's own W04 fix dropped `or` defaults for `port`/`base_url` — rewritten as `inherit ({ defaults } // solidPodRsCfg) port base_url`; W01 `== true` normalisations kept as `builtins.isBool v && v`. NOT parse-checked here (no nix in container) — `nix flake check` at the agentbox rebuild is mandatory before landing. Warned the sidestr session to audit its pushed statix commit e1ba33a1c for the same default-drop.
- **Owner**: `/home/devuser/workspace/project/.git/config` carries `fetch = +refs/tags/*:refs/remotes/dreamlab-github/tags/*`, which recreates 43 tag-mirror remote branches on every fetch (reported by the sidestr session). Remove the line if unintended.
