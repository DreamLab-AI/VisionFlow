# P2 — F9: Federation rescope fork (canon)

| Field | Value |
|-------|-------|
| Register item | F9 federation rescope fork (canon reconciliation, P2) |
| Canon owner docs | `docs/ADR-005-gap-close-canon-decisions.md` §Decision 6; `docs/PRD-gap-close-canon.md` §"Federation Rescope Fork (F9)" |
| Fork record | `docs/registers/F9-federation-fork-record.md` (the register-trail artefact) |
| Canary | none — a canon reconciliation / rescope decision, acceptance check not a canary |
| Branch | `gap-close/2026-07` |
| Base commit at work start | `b47c5fd652eaf5306118dbf9a511e31a74755b33` |
| Maturity (honest) | Fork **evaluated → federation stays `planned`** (0/3 criteria hold). The fork criteria are written (`standalone`); federation is not built and is not claimed above `planned`. |

## What was decided

The three ADR-005 §Decision 6 build criteria were evaluated against current tree
truth. **None holds → the fork resolves to park; F9 stays `planned`.** Recorded as an
immutable fork record in the register trail (`docs/registers/F9-federation-fork-record.md`),
forward-chaining into the next `RegisterKeeper` cut (v1.2, P2 boundary); it does **not**
edit `gap-register-v1.1.md`, `PRD-gap-close-sprint.md` or the closeout register in place
(Invariant 5).

| # | Criterion | Holds? | Evidence (file:line) |
|---|---|---|---|
| (a) | ≥2 independent-operator relays requiring cross-relay 31400–31405 | **NO** | `docs/architecture/compatibility-matrix.md:11` (federated-by-default tally 1/4); `docs/ecosystem-map.md:117`; `docs/protocol/event-kind-registry.md:84,88` (single relay allow-list) |
| (b) | Ratified `MeshTransport` contract against IS-Envelope | **NO** | `docs/architecture/compatibility-matrix.md:11` (`nostr-bbs-mesh` scaffold-only, no `MeshTransport` impl); `docs/ecosystem-map.md:119` (IS-Envelope routing unimplemented in every substrate — only conformance vectors exist) |
| (c) | Standalone-first freeze explicitly lifted | **NO** | `docs/ecosystem-map.md:115` (G2 FROZEN), `:121` (closeout FREEZE); `docs/ADR-005-gap-close-canon-decisions.md:88` (freeze taken as input, not reopened) |

## Receipt

```
$ git rev-parse HEAD
b47c5fd652eaf5306118dbf9a511e31a74755b33

# (a) single-operator mesh, federated-by-default tally 1/4
$ grep -n "federated-by-default tally is \*\*1/4" docs/architecture/compatibility-matrix.md docs/ecosystem-map.md
docs/architecture/compatibility-matrix.md:11: … the federated-by-default tally is **1/4**, not 2/4 …
docs/ecosystem-map.md:117: … The federated-by-default tally is **1/4, not 2/4**.

# (b) no MeshTransport impl; IS-Envelope routing unimplemented everywhere
$ grep -n "no \`MeshTransport\`\|IS-Envelope routing (ADR-075) is unimplemented" \
    docs/architecture/compatibility-matrix.md docs/ecosystem-map.md
docs/ecosystem-map.md:119: … no MeshTransport implementation … IS-Envelope routing (ADR-075) is unimplemented in every substrate …

# (c) freeze in force, not lifted
$ grep -n "Standalone-First Is the Supported Mode (FROZEN)\|Closeout decision (FREEZE)" docs/ecosystem-map.md
docs/ecosystem-map.md:115:### G2: Mesh Federation Is Scaffold — Standalone-First Is the Supported Mode (FROZEN)
docs/ecosystem-map.md:121:**Closeout decision (FREEZE):** declare **standalone-first** as the supported deployment mode …
```

## Acceptance mapping (PRD §"Federation Rescope Fork (F9)")

| Criterion | State |
|-----------|-------|
| Fork criteria written and testable (reviewer can decide build-or-park) | Done — three predicates evaluated with file:line evidence |
| Rescope decision recorded in the register trail with the criteria evidence | Done — `docs/registers/F9-federation-fork-record.md` |
| Federation stays `planned`; forum ships nothing; 38xxx kinds stay off allow-list | Recorded — 0/3 hold → park |
| Not edited into the published v1.1 register in place | Honoured — forward-chained fork record (Invariant 5) |

## Falsification exposure

Falsified if any of the three criteria in fact holds against the cited evidence and
F9 is nonetheless parked; if federation is claimed built or the 38xxx kinds added to
`MESH_FEDERATED_KINDS`; or if the decision edits the published v1.1 register in place
rather than chaining forward.
