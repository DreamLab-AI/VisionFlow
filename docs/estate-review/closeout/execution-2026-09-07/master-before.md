# Pre-execution master board snapshot

Historical source: `project/docs/TODO-unified.md` at execution start. Relative links inside the fenced snapshot retain their original source base.

````markdown
# Unified TODO - VisionClaw + agentbox

**Status:** Living document, single combined register. Reconciled **2026-09-06** against the 2026-09-01..06 sprint using the estate evidence pack: resolved rows removed with evidence (bottom), partial rows rewritten to say exactly what landed and what remains, stale citations repointed at the repo that actually holds the record. The 2026-09-02 slim board is in git history (`git show 3ea6e9e:docs/TODO-unified.md` or the commit preceding this one); the 2026-08-31 long form is at `git show e8fab1098:docs/TODO-unified.md`.
**Governed by:** [PRD-024 Final-Mile Closeout](archive/prd/PRD-024-final-mile-closeout.md), [ADR-133](archive/adr/ADR-133-final-mile-sprint.md)
**Rule:** closures cite evidence; remove rows when done. Rows with mixed states must identify the remaining dependency. The numerical counts below are historical 2026-09-06 reconciliation figures, not a fresh machine count. Open count: **42 before (recount; the previous header's "40" combined the paired L-6+L-1 and DR-1+DR-2 rows into one each), 37 carried + 9 new = 46 after, then G-20 closed the same day: **45 open** (6 removed, see bottom). Evening reconciliation 2026-09-06 (estate mesh, session 017UpCFJ): +7 new rows G-23..G-29, V-1 narrowed; **52 open** (12 removed).
**Repo-qualified IDs:** agentbox and VisionClaw independently mint `ADR-2002`, `ADR-2016`, `ADR-2017` and `ADR-2031` for unrelated topics. Every ADR reference below is prefixed `VC` or `AB`. Fixing the collision itself is G-14.

| State | Meaning | Unblocks when |
|---|---|---|
| `code-gap` | Code never written or half-shipped | An agent tick writes it |
| `ops-action` | Infrastructure/config action, minutes of work | Operator (or authorised tick) performs it |
| `live-session` | Code+test proven; needs observation on real traffic | An operator tock drives the session |
| `posture` | Deliberately held closed; a decision, not a gap | Operator decides (deciding "stay closed" also closes it; re-date) |
| `data-floor` | Waiting on samples/corpus to accumulate | The clock; check floor, then flip |
| `external` | Blocked outside both repos | Upstream/network change |

## 2026-09-07 audit closeout — current priority overlay

The [source audit](../../VisionFlow/docs/estate-review/2026-09-07-estate-audit.md) and [sprint acceptance plan](../../VisionFlow/docs/estate-review/closeout/2026-09-07-sprint.md) extend this board. These seven new rows are open implementation/acceptance work; documentation changes do not close them. Existing rows retain their IDs and historical evidence. Roles are responsible maintainer functions, not new personal assignments.

| ID | State | Required closeout | Owner role / dependency | Evidence |
|---|---|---|---|---|
| EA-01 | `code-gap` | Exclude private ancestor identifiers and inferred material from every public export in the actual visionGraph producer; reconcile extracted knowledgeGraph implementation | Publisher maintainers; establish shipping source | Federation audit F-03: isolated private-grandparent fixture leaks into inferred RDF; no production incident claimed |
| EA-02 | `code-gap` | Atomic ontology generation activation or proven equivalent; fail closed on ACL existence errors; write-failure/restart/readability tests | VisionClaw/pod storage maintainers; reader/storage boundary | VC ADR-2106 now partial; `ontology_pull.rs:345-373` sequential writes and `unwrap_or(false)` ACL probe |
| EA-03 | `code-gap` | Commit trust audit only when guarded state transition succeeds; test zero-row and concurrent level/activity/exemption changes | Forum relay/D1 maintainer | Forum ADR-2006 annex; exact-query SQLite probe commits false audit with UPDATE changes=0 |
| EA-04 | `code-gap` | Complete governance request/case/operation correlation, guarded legal transitions and affected-row checks, replay and external applied receipts | Forum/VC/AB governance maintainers; durable store and acknowledgement semantics | Forum ADR-2010 remains proposed/partial/inactive; source receipt batching exists but does not close the journey |
| EA-05 | `code-gap` | Verify Loom served generation at Agentbox consumer, bind caches/model/corpus and reject mismatches | Loom/retrieval maintainers; EA-01/02 and G-19 | AB ADR-2075 annex: `/loom/generation` exists; retrieval client does not consume it |
| EA-06 | `code-gap` | Effective-profile store membership for erasure/restore, including optional Redis sessions; idempotent partial-failure reconciliation | Data/operations maintainers; configured authority inventory | VC ADR-2004/DATA-authority qualification; canonical Oxigraph/SQLite does not mean all persistence is local |
| EA-07 | `code-gap` | If programmable RuView is selected, authenticate uploaded packages cryptographically and bound nonterminating WASM execution; otherwise explicitly exclude that profile | RuView firmware maintainer; supported-profile decision | Sensing audit: public-input digest comparison is not Ed25519, post-return elapsed-time measurement is not preemption |

Custody G-5/G-6/G-17 is a prerequisite for exposure. G-16 remains a numerical GPU blocker until its target oracle passes. M-3 and D-1 require loaded-runtime/current-corpus evidence; no rebuild or recall measurement occurred in the documentation audit. Source/ADR/index corrections are recorded in the audit, not counted as system acceptance.

## Next items, priority order (dependency first, then value, then cost)

| # | ID | State | Item | Size | Why here |
|---|---|---|---|---|---|
| 1 | G-5 | `code-gap`+`ops-action` | Publisher-key split (ADR-040 D3, AB ADR-2027): `agentbox.toml:117` `[sovereign_mesh.operator].pubkey_hex` still literally carries the shared visionclaw-server key `b41654017f...682f7a`; the split has not landed. AB `services/secret-backup` (commit `7905d2a64`) now provides age-encrypted custody and rotation tooling, so the missing piece is performing the split, not building the tool | M | Must land before any relay is exposed; compromise window is one build cycle, so ride the M-3 rebuild |
| 2 | G-6 | `code-gap` | Abort boot on failed key derivation. Half landed: `management-api/lib/agent-identity.js` `mint` now fails closed with a non-zero exit (AB ADR-2044). Still open, the row's literal ask: `config/entrypoint-unified.sh:902,922` still preserves `AGENTBOX_AGENT_DID="${AGENTBOX_AGENT_DID:-did:nostr:local}"` on a failed or invalid mint, so boot continues with a placeholder DID | S | Quick win; stops the new HP node minting a placeholder DID into the mesh |
| 3 | M-2 | `code-gap`+`ops-action` | ml relay expose for the rail: `[sovereign_mesh.relay]` still `bind = 127.0.0.1`, `expose = false`, no rail address baked. Citation refreshed: `flake.nix:1471,2195,2672` are the live bind/publish sites (the old `:2269-2270` drifted with file growth). Needs the ml side of `10.10.10.0/30`, the HP node pubkey in `allowed_pubkeys` on both nodes, and `ingress_policy = "allowlist"` kept | S | The concrete config the rebuild bakes; scoped to the rail, not the LAN |
| 4 | T-1 | `posture` | Relay exposure chain decision: expose then mobile bridge (`[sovereign_mesh.mobile_bridge].enabled = false`, unchanged this sprint). Allowlist is baked and live (`AGENTBOX_ALLOWED_PUBKEYS` populated) | S | Formal decision M-2 executes; decide once with rail-only scope so it is not reopened per node |
| 5 | M-3 | `ops-action` | Agentbox image rebuild round 3. ml half is DONE (`08e817f39`, `ddd1f1ec8`, which also resolved three rebuild-blocking bugs found by `./agentbox.sh rebuild`). Remaining: repeat on HP, for which no receipt exists | S | ~15 min host op that gates every downstream mesh item |
| 6 | M-4 | `code-gap` | Federation Phase 3 fan-out. Landed: `mcp/nostr-bridge/relay-consumer.js` now has a real `RelayConsumer` class with `subscribe`/`unsubscribe`/`fanout` modes, so "no code consumers" is no longer true. Remaining: `AGENTBOX_RELAY_FANOUT` still defaults `'off'` at `management-api/server.js:1328` (was cited `:1308`) although `agentbox.toml:161` says `bidirectional`; the `[mesh]` toml keys are still not wired to the constructor, and no `[federation]` table exists | L | The code that makes two relays talk; can start in parallel with 1-5, cannot be verified before M-3 |
| 7 | M-5 | `posture`+`ops-action` | Dream sovereign-mesh slot: `services/nostr-pod-bridge/Cargo.toml:21-22` still path-deps `../../../../{nostr-rust-forum,solid-pod-rs}`, escaping the per-night clone. Confirmed from the other side: neither sibling references an annexe path, so this is purely an agentbox-side posture decision, not made | S | Unblocks the nightly mesh slot on the new node; decision only |
| 8 | L-6+L-1 | `live-session` | Mobile bridge e2e on phone (Amethyst+Amber, note-to-self thread) and the envelope canaries (voice-intent, MAST tag, 31403 release, CTC cost, URN resolve, DID federation proof). No artefacts this window; inherently a live-session row a repo grep cannot move | M | First real traffic through the exposed relay; observation, no code |
| 9 | G-4 | `posture` | Session-mirror egress boundary (AB ADR-2026). Code is DONE: `config/hooks/nostr-live-mirror.cjs` now runs `redactForEgress(body)` before send and fails closed on null (commit `11804ba4b`). Remaining is governance only: AB ADR-2026 is still `proposed`/`inactive`, and a stale "fail-open everywhere" doc-comment survives nearby and should be deleted | S | Code hole is shut; the record still lies about it |
| 10 | DR-1+DR-2 | `ops-action` | Re-run `/dream` on nostr-rust-forum and break the 7-night INCONCLUSIVE streak. **Premise corrected 2026-09-06:** there is no VisionFlow `ADR-0057` anywhere in the repo (the prior row's ratification ask was based on a coincidental hash substring), and the "bench fix is committed" claim was wrong: `486ec5a` touches feature-gate, identity, D1-migration and trust-sweep work with zero hits for `dream.config.json` or any bench/perf evaluator path, while the real bench commits `d0bc337` and `f18b471` predate the sprint window. `docs/dream-cycle/LEDGER.md` still ends at the same 7 rows (2026-08-15 to 08-21, all INCONCLUSIVE); `/dream` was not re-run. The remaining ask is to run it, plus the separate "mock Darwin is smoke-class" decision | S | Independent of the mesh, cheap, restores nightly engine value |
| 11 | G-16 | `code-gap` | **New.** GPU analytics LOF kernel is measured BROKEN: `crates/visionclaw-gpu/tests/analytics_oracle_conformance.rs::adr_2061_lof_matches_oracle` fails at max per-point delta 0.702 against a 1e-3 bar, 702x over, on an A6000. Root cause recorded in `docs/GPU-wire-abi.md:176`: query k-distance used in place of neighbour k-distance. PageRank, DBSCAN and Louvain are TRUSTED. Gross outliers still flag but inlier ordering is wrong | M | A shipped kernel with a measured wrong answer and a known root cause; cheapest high-value fix on the board |
| 12 | E-5 | `external` | **New, register gap.** KG elevation and direct ontology axiom load (`sovereign_mesh.kg_elevation`, `skills.ontology.direct_axiom_load`) blocked on `visionclaw-server:4000` being unreachable from agentbox. Listed in `agentbox/docs/developer/backlog.md:51` since 2026-07 but never carried into this register | S | Tracked in one repo only for six weeks; surfacing it is the whole fix until the service is reachable |

## Carried open entries (compressed)

**Mesh follow-ons:** LM-1 `posture` (S) Loom confidence-surfacing contract. Implemented and tested but unratified: loom `c86f462` ships `crates/loom-domain/src/grounding.rs`, `routes/grounding.rs` (+226), `routes/health.rs` (+53) and a 453-line `exp014_grounding_contract.rs` covering ADR-138 D1-D4. Loom ADR-138 is still `Status: Proposed`; ratifying it closes this row.

**ADR governance (§9):** G-1 `code-gap`+`posture` (S) **qualified 2026-09-07:** boot rejection and the `dev-auth-release-gate` shell checks are implemented, but those source checks do not prove every produced release artefact excludes dev-auth. VC ADR-2037 remains proposed/none/inactive; acceptance requires the actual artefact's feature configuration and negative authentication probes. ADR ratification is a separate owner decision; do not close this as a record-only edit. G-2 `code-gap` (S) mostly landed: `assert_effective_profile_or_exit(...)` is wired into `src/main.rs:873-876` before `HttpServer::new`/`.bind()` (commit `b0bc275f6`, VC ADR-2038 now `partial`/`live`), and VC ADR-2043 closes the illegal `RBAC_PUBLIC_READS=1` + `PUBKEY_VISIBILITY_FILTER=0` combo; remaining per ADR-2038's own text is that an undeclared or unnamed profile combination still raises no finding and still binds. G-7 `code-gap` (L) **citation repointed**: the target is **VC** `docs/adr/ADR-2016-provenance-append-only.md` and `ADR-2017-write-master-backup-posture.md` (both still `implementation_status: partial`), not the unrelated agentbox 2016/2017 Wilson-bound and consumer-ordering records. No backup or erasure path landed; VC opened four new *proposed* ADRs (2102-2105) re-scoping rather than closing. AB `services/secret-backup` is secrets custody, not provenance backup, and does not satisfy this row. G-8 `code-gap` (M) per-pubkey admission ahead of `ReplayCacheFull`; `src/utils/nip98.rs:198` is still `REPLAY_CACHE_MAX_ENTRIES = 100_000` fail-closed at `:272`, TTL and capacity only. G-10 `posture` (M) LAN-door threat model: VC ADR-2062 extends the exposure gate to container-internal `0.0.0.0` listeners and the literal bind count in `flake.nix` is down from nine to **4**; raw CDP `9222` sits on a separate sidecar explicitly out of that gate's scope, and no cross-cutting threat-model doc exists. G-11 `posture` session-bearer sunset, still blocked on React client per-request signing, untouched. G-14 `code-gap` (L) supersession graph, **materially worse**: **97 of 98** `docs/adr/ADR-*.md` records still carry `supersedes: []`, and the corpus grew from the 42 this row originally cited to 98 because Wave 3 and the closeout minted ADR-2043 through ADR-2105 with no lineage backfill, so the absolute gap widened. The 2026-09-07 coverage index now repository-qualifies ADR references. Cross-record supersession/backfill remains open; namespace correction alone does not complete lineage. G-15 `posture` status-axis lattice, no schema or doc change found.

**Live-session (§3):** L-2 diversity canary (needs a second model family). L-3 VC ADR-117 clamp fire, record still archived, no fire event log. L-4 VC ADR-119 telemetry fire, record still archived, no fire evidence. L-5 on-headset XR: the closeout XR pass reports 218 local XR library tests pass and states explicitly that this does not certify Godot runtime or deployment, so on-headset remains unevidenced (folds X-6).

**Posture (§4):** T-4 held surfaces, all 8 rows in `agentbox/docs/developer/backlog.md` unchanged: git pods/host gateway, payments (until counterparty), Solid OIDC issuer, pod MCP, kernel pip, relay exposure, mobile bridge, multi-user. solid-pod-rs `40f160c` adds an OIDC compat matrix and its own ADR-2003 "defer LWS-1.0", which documents *why* the issuer stays deferred rather than un-deferring it; `integrations.solid_pod_rs.enable_mcp = false` unchanged. Re-date.

**Data-floor (§5):** D-1 `data-floor` flip `feed_routing` after the observation window. **Contradiction resolved by fresh measurement 2026-09-06:** `./agentbox.sh ruvector recall` returns **PASS, exit 0**, median self-recall **189/200** (band >=175) and true-recall **115/120** (band >=102) over 3 runs at k=10, against corpus `corpus-9992e88f6165` (196,949/196,949 embedded, `idx_memory_embedding_hnsw` m=16 ef_construction=128), fixture `sha256-6ace8c1d...`, receipt `sha256-12-cbccf57f8d35`, artifact `agentbox/backups/ruvector-sidecar/recall-runs/2026-09-06T08-55-47-514Z.json`. This confirms agentbox `fa024cc08` and supersedes the closeout addendum's 164/200 and 96/120 FAIL, which measured the index before the serial HNSW rebuild. The gate is therefore not blocking. What remains is the row's original ask: `agentbox.toml:417-418` still has `feed_retrieval = true`, `feed_routing = false`, and the observation-window length is still undefined in `docs/LEARNING-memory.md:169`. Define the window, then flip.

**External (§6):** E-1 ComfyUI, still no container on `visionclaw_network`. E-4 Nagual QE, upstream sqlx 0.9 `SqlSafeStr` still unpinned in `agentbox/lib/nagual-qe.nix`. E-5 KG elevation, see priority row 12.

**Docs (§12):** DOC-1 `docs/reference/rest-api.md` broker/workflow phantom mentions reproduce at exactly **34**, unchanged. DOC-2 kernel count drift only: `__global__` now **82** (was 83), while `docs/explanation/actor-hierarchy.md:201` and `:603` still both say "37"; the mismatch persists at two sites, not one. DOC-3 `docs/tutorials/installation.md:66-92` still walks a docker-compose v1 binary install while `scripts/launch.sh` uses v2 `docker compose` throughout. DOC-4 **citation repointed** (S): the record is **AB** `docs/adr/ADR-2002-aoe-token-auth-boundary.md`, not VC's unrelated ADR-2002 nip98 replay-cache record. It picked up a fresh `verified_commit` (`796d85fcf`) this sprint but is **still `activation_status: staged`**, so the ask (flip to active, reconcile with AB-2009) is untouched. **DOC-5 (new, S)** Loom model-name drift across three files: `agentbox/agentbox.toml:1708` says `loom_model = "qwen3.8-27B"` and the workspace CLAUDE.md says Qwen3.8-27B, while `loom/docs/QWEN3.8-CONNECTION.md:16-19` records that since 2026-08-18 the deployed variant is the **Heretic abliterated Qwen3.8-27B Q8_0** GGUF (262K context, q8_0 KV, ~19.5 tok/s), which also ships with no embedded chat template. Pick one name and propagate.

**Branches:** C-11 `ops-action` (S) **counts corrected**: `project` now has **10 local branches and 7 worktrees** (the row cited 27/24, from the pre-regeneration pool); agentbox has 10 local. The three VALUABLE branches (`refactor/kg-node-rename`, also tagged `archive/kg-node-rename`; `report/soundings-qe-audit`; `impl/khive-investigation`) are confirmed remote-only, matching the local-deleted/remote-kept description exactly. The remote keep-or-drop decision is the only thing still open.

**Obsidian corpus residue (from C-14 close-out):** V-1 `code-gap` (M, visionGraph) pipeline reds: `DUPLICATE_IRI` (`Open Source AI.md`/`Open-Source AI.md`), 102 orphan refs beyond baseline, ADR-NG-002 P2 Loom reload trigger, `/notes` SPA rebuild. Not independently re-located this pass; verify the artefact before acting. V-2 `code-gap` (S) core fix DONE: commit `9423abdb3` (2026-09-02 12:28Z) anchors the isolated-node peripheral shell to the configured bounds rather than the live AABB. Remaining are the two named follow-ups, neither found: the `integrate_pass_kernel` hard clamp and the connected-only AABB. V-3 `code-gap` (S) NIP-98 still verified twice on `POST /api/bots/update` giving `401 Token replayed`; mechanism unchanged, no route-specific fix. V-4 `code-gap` (S) two parts: the self-contradictory `dag_rank_tests::directed_hierarchy_relation_accepts_only_class_subsumption` assertion is addressed via VC ADR-2035, and `8e0ee41bb` (2026-09-02) percent-encodes repo paths, but `GitHubClient::get_full_path`'s own prefix heuristic is unchanged and still needs an owner. Line citation drifted from `force_compute_actor.rs:4563` to roughly `:4655-4700` as the file grew; re-confirm before closing. V-5 doc (S) diagram PNGs 01/04/06/07 still Logseq-labelled; `Two Heads Are Better Than One.md` corrupted `title:`. **Narrowed 2026-09-06 evening:** the `DUPLICATE_IRI` is fixed (visionGraph `dec15265c`), the orphan refs are absorbed into a regenerated ADR-NG-002 P1 baseline (`8f9874e59`, 5,266 baselined, 0 new), the mirror gate now excludes `_misc/` to match `UNPUBLISHED_DIRS` (same commit), and **narrativegoldmine.com deployed for the first time** (publish.yml run 34049824503, all gates green, `ACCESS_TOKEN` set as a fine-grained PAT scoped to knowledgeGraph Pages; browser-verified 8,432 classes / 102,244 resolvable edges). Remaining in V-1: ADR-NG-002 P2 Loom reload trigger and the `/notes` SPA rebuild only.

**Estate hygiene (new 2026-09-06):** G-17 `code-gap` (S) agentbox `services/secret-backup` (AB ADR-2027) is built and tested but `grep -c secret-backup flake.nix` returns **0**, so a real custody capability sits outside the reproducible build. G-18 `code-gap` (S) **partly addressed 2026-09-07:** VisionFlow `scripts/diagram-index-gen.cjs --cite-check` resolves declared source suffixes and checks line bounds, with advisory symbol checks. It warns on unresolved/ambiguous citations and does not certify semantics or source revisions; hosted CI skips sibling source paths. The coverage generator now labels revisions as declarations and qualifies ADR keys by repository; its collision regression test passes. Remaining: meaningful citation diagnostics, governed source identity and source-to-claim review evidence. The legacy `validate-mermaid-diagrams.sh` is a separate, stale script, not the active topic-tree gate. G-19 `code-gap` (S, loom) loom has no `.github/workflows/` directory at all, so nothing in it is gated by hosted CI, and `loom/Cargo.toml:60` carries an out-of-tree path dependency `ruvector-core = { path = "../ruvector/crates/ruvector-core" }` that no clone or CI runner can resolve without the sibling checkout. The sibling `/home/devuser/workspace/ruvector` sits on branch `report/agentbox-field-2026-07`, which exists on the `jjohare/ruvector` fork (`677b2475`) but **not** on the `ruvnet/ruvector` upstream, so the Loom builds against a fork-only branch through an unversioned path. Adding CI would surface this immediately. G-21 `ops-action` (S) `DreamLab-AI/dream-engine` has 6 open dependabot PRs, #1 to #6, all opened 2026-08-16 and 21 days stale as of 2026-09-06 (configure-pages 5 to 6, a 7-update dev-dependencies group, codeql-action 3 to 4, upload-pages-artifact 3 to 5, setup-node 4 to 7, deploy-pages 4 to 5). Triage, merge or close them. G-22 `code-gap` (M) GitHub Actions steps are pinned to mutable tags rather than 40-character commit SHAs across most of the estate, which leaves those workflows open to tag repointing upstream. Measured 2026-09-06 as SHA-pinned of total external `uses:` refs: VisionClaw 0/42, agentbox 5/60, VisionFlow 0/19, nostr-rust-forum 0/27, prose-sanitiser 0/7, versus solid-pod-rs 33/33 and dreamlab-ai-website 57/57 fully pinned. loom is not an exception, it has no workflows at all (G-19). The two fully pinned repos are the pattern to copy.

**Estate check follow-ons (2026-09-06 evening, surfaced by the new nightly estate check — VisionFlow ADR-2008, live at www.visionflow.info/#estate):** G-23 `ops-action` (S) add repo secret `ESTATE_READ_TOKEN` on DreamLab-AI/VisionFlow (fine-grained PAT, Contents/Actions/Metadata read on `jjohare/visionGraph` only); until then the visionGraph row reads `unreadable · repository not readable: HTTP 404` every night. G-24 `code-gap` (S) VisionClaw main red: `Documentation Quality CI` failure (run 34047570303) and `XR Godot CI` cancelled (run 34029044722); triage both, they will be RED lines in every dream night until fixed. G-25 `code-gap` (S) agentbox main red: `Contract tests` failure (run 33997923773); the second agentbox RED (`Ontology Federation CI/CD`) clears itself, that stale workflow was deleted (`7b2811272`). G-26 `code-gap` (S, VisionFlow site) `initScrollReveal` uses `threshold: 0.1`, so any section taller than ~10x the viewport can never intersect 10% and stays at `opacity: 0`; `#estate` opts out in CSS. Page-level fix is `threshold: 0` plus a `rootMargin`; retest the scroll choreography of the other 16 sections. G-27 `posture` (S, dream-engine) CI matrix is now Node 20/22/24 (18 dropped: EOL, unsupported by eslint 10 / vitest 4) but `packages/cli` still declares `engines >=18`, which CI no longer verifies; raise to `>=20` or add a leg. G-28 `code-gap` (S, dream-machine local) the uncommitted `LEDGER.md` edit in `~/workspace/dream-machine` fails `packages/ledger` `rowContract` (`finding-hypothesis-leak`, 2026-09-06 row); the committed version passes; fix before it lands. G-29 `posture` (S, loom) model-identity doc: workspace notes and agentbox config name Qwen3.8-27B while loom docs say the served build has been the Heretic-abliterated Q8_0 variant since 2026-08-18 (closeout-table G-18); a trust-boundary statement is the operator's to write, and it should name which one the Loom facade actually serves.

**Frozen (§7), unchanged:** ADR-073..085 window, ADR-122/123, RVF file store, XR APK cross-build. §11 design open-questions remain pointers.

## Removed as resolved (with evidence)

| ID | Evidence |
|---|---|
| G-20 agentbox solid-pod-rs pin skew | Resolved 2026-09-06: solid-pod-rs `v0.5.0-alpha.9` (`1d9da527`) cut and all eight crates published; agentbox `cda60e7a0` pins `lib/solid-pod-rs.nix` and `lib/nostr-pod-bridge.nix` to that tag with one SRI hash, vendors the tag's Cargo.lock, and the bridge lockfile resolves `solid-pod-rs-nostr 0.5.0-alpha.9`; superseded registry versions yanked. Follow-up: the forum kit still pins root `solid-pod-rs =0.5.0-alpha.7`; it compiles against alpha.9, so the next kit release moves it |
| M-1 HP-Desktop peer node observation | `agentbox/docs/developer/hp-peer-node.md` records the real-traffic probe the row was waiting on: the `:9096` NIP-98 door HP to ml returned 401 `pubkey_not_allowed`, then 200 after allowlisting. That is the first real cross-node exchange this row was held for |
| M-6 HP-ml handshake + allowlist rejection | Same `agentbox/docs/developer/hp-peer-node.md` evidence covers both halves: the successful handshake above, and the `:7777` relay ml to HP key correctly rejected by `nostr-pod-bridge` while unlisted |
| G-3 cross-repo federation CI fixture | agentbox `117f94bf5` shipped `schema/federation-kinds.json` plus a JS contract test and a Rust `include_str!` parity test, both wired into CI, governed by AB ADR-2061 (`activation_status: live`). The row's "sha12" grep target no longer exists anywhere: ADR-2061's schema-file approach superseded that terminology, it did not skip the fixture |
| G-9 Loom `:8084` LAN-reachability audit | AB ADR-2070 ratified this sprint (`796d85fcf`) declares the Loom raw `:8085` backend and the `:8084` facade a deliberate, bounded, named egress door, which is the audit this row asked for |
| G-13 PTX `.version` rewrite | `crates/visionclaw-gpu/src/ptx_loader.rs` now delegates to a single shared span-based `.version` rewrite used by both the loader and `build.rs` (comment cites "ADR-2030 closeout"). The fixed-window splice that failed on unexpected headers is gone |
| RuVector recall-gate contradiction (never a row) | Raised by the closeout addendum as a live FAIL (self 164/200, true 96/120, gate exit 3) contradicting agentbox `fa024cc08`'s claimed PASS. Resolved by fresh measurement 2026-09-06: PASS, exit 0, self 189/200, true 115/120, receipt `sha256-12-cbccf57f8d35`. The FAIL measured the index before the serial HNSW rebuild. Recorded here so it is not re-raised; the residual observation-window work stays as D-1 |
| T-5 remote `crashbug` delete | `git ls-remote --heads dreamlab-github` lists 10 heads, none `crashbug`; tag `archive/crashbug` present |
| T-6 rebuild round 2 | Live: `ffmpeg` resolves to `/nix/store/...-ffmpeg-8.1.2-gpuwrapped`; `/opt/agentbox/skills/tree-search-coder` present; baked entrypoint carries `VAULT_ROOT`; `rune-1.4.0` in `/nix/store`; tmux window 9 "Notes"; baked `management-api/{server.js,routes/agent-events.js}` sha1-identical to d392bd4c2. Round 3 is M-3 |
| G-12 AoE token activation | Running process: `aoe serve --auth token --behind-proxy --host 127.0.0.1 --port 9095` (agent-of-empires-1.13.2; `flake.nix:1993`). Residual doc flip is DOC-4 |
| C-14 Obsidian migration owner steps | `.env:12,15` `GITHUB_REPO=visionGraph`, `GITHUB_BASE_PATH=knowledge/pages,working/pages`; submodule bump cfa20a73b, merge 619b439be, e8fab1098; `visionclaw_container` created 2026-09-02T11:11Z after the 10:37Z merge; agentbox rebuilt (see T-6 row); `gh repo view jjohare/logseq` gives `isArchived: true`. Residue split into V-1..V-5 |
| C-13 logseq narrativegoldmine follow-ups | `jjohare/logseq` archived (`isArchived: true`); publish pipeline now lives in `jjohare/visionGraph`; live residue (ADR-NG-002 P2, IRI reds) carried as V-1 |
| C-9 / X-1..X-5 / T-2 / T-3 (already struck in source) | Confirmed done per source; dropped from the slim board as non-open |
| dreamlab-ai-website PR #49 kit-pin-guard CI | Merged 2026-09-02 (lead context); was not a register row, noted so it is not re-added. Note that the later commit `000a40e` shipped further checker, pin-parity and chat-turns work on that repo, which is distinct from this row and is not itself a gap |
| VisionClaw `ontology-publish.yml` never ran (closeout row 23; not previously a register row) | 2026-09-06: red on every run since 2025-12-31. Unmasked and fixed in sequence: `ONTOLOGY_SOURCE_TOKEN` secret, archived default source (`c0b92d5f4`), pip cache (`416546ddf`), unreachable pod deploy gated on `vars.SOLID_POD_URL` (`9de8344e1`), inline converter that shipped 0 `owl:Class` replaced by the vault's own `pipeline.build` (`8e3cc92ae`, 8,434 classes / 265,455 triples), and pod delivery inverted to a pull model from release `ontology-latest` (VC ADR-2106, `cbf4097b1`, run 34047570300). Recorded so it is not re-raised |
| Estate PR stack (not a register row) | 2026-09-06: 0 open PRs across all 9 DreamLab-AI repos. dream-engine dependabot #3/#4 merged; VisionClaw #16/#17, VisionFlow #1, agentbox #4 closed as stale with conflicts; branches deleted where permitted |
| dream-engine main red (surfaced by the estate check) | 2026-09-06: red since 12:04Z on dependabot #2 (TypeScript 7 has no typescript-eslint release), not the later action bumps. Fixed `1ab31e8` (TS ^6, `"types": ["node"]`, Node 20/22/24 matrix) and `9f4d8a9`; Pages enabled by the operator. CI 34058001661, Deploy Pages 34058001618, CodeQL 34058001633 all green; https://dreamlab-ai.github.io/dream-engine/ serves 200. Residue is G-27 |
| WasmVOWL / webvowl-wasm duplication (closeout row 22) | 2026-09-06: engine published as crate `vowl-wasm` 0.1.2 and npm `@dreamlab-ai/vowl-wasm` 0.1.2 via OIDC trusted publishing; `@dreamlab-ai/webvowl-wasm` deleted from npm; visionGraph pins the npm semver (`b51a9cf8c`); knowledgeGraph cut over (`3191d3ed5`). Copy A (WasmVOWL demo shell) deliberately held pending a positions accessor on `WebVowl` |
| Hand-maintained closeout table as the estate view | 2026-09-06: superseded by the nightly estate check (VisionFlow ADR-2008): `scripts/estate-health.mjs` collects 14 repos, 6 surfaces, 7 registries at 02:30 UTC, commits as `estate-health[bot]`, dispatches deploy.yml explicitly, and the 03:00 dream night reads it offline (`ESTATE-HEALTH-OK|STALE|RED`). Proven end to end twice (runs 34055073094, 34057985914). First real snapshot: 8/14 green |

````
