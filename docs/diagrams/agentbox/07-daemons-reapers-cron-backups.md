---
id: AB-07
title: Daemon classes, argv-boundary reaping, cron and backups
area: agentbox
governing:
  - ../project/agentbox/docs/BASELINE-container.md
adrs: [ADR-2032, ADR-2003, ADR-2039, ADR-2040]
sources:
  - ../project/agentbox/docs/BASELINE-container.md
  - ../project/agentbox/services/agentbox-ops/src/procs.rs
  - ../project/agentbox/services/agentbox-ops/src/process_identity.rs
  - ../project/agentbox/services/agentbox-ops/src/bin/ruflo-daemon-gc.rs
  - ../project/agentbox/services/agentbox-ops/src/hermes/mod.rs
  - ../project/agentbox/services/agentbox-ops/src/hermes/jobs.rs
  - ../project/agentbox/flake.nix
  - ../project/agentbox/agentbox.sh
  - ../project/agentbox/scripts/ontology-condense-scheduler.mjs
  - ../project/agentbox/scripts/ruvector-aggregate-sweep.mjs
  - ../project/agentbox/scripts/ruvector-pattern-distill.mjs
  - ../project/agentbox/aisp/cli.js
verified_commit: 2c521c5bb
---

## AB-07.1 Supervised process classes by lifecycle shape
```mermaid
flowchart TD
    SUP["supervisord PID 1 root<br/>generated by flake.nix supervisorText"] --> C1["CLASS A — self-looping node daemons<br/>script invoked with --loop, autorestart=true, startsecs=0"]
    SUP --> C2["CLASS B — supercronic cron runners<br/>schedule lives in a crontab OUTSIDE the image"]
    SUP --> C3["CLASS C — long-lived services<br/>see AB-02.4 to AB-02.7 for the full tree"]
    SUP --> C4["CLASS D — one-shots<br/>bootstrap and bootstrap-seal, see AB-02.8"]
    C1 --> A1["ruvector-aggregate-sweep flake.nix:1879<br/>node scripts/ruvector-aggregate-sweep.mjs --loop, priority 232"]
    C1 --> A2["ruvector-pattern-distill flake.nix:1908<br/>node scripts/ruvector-pattern-distill.mjs --loop, priority 233"]
    C1 --> A3["ontology-condense-scheduler flake.nix:1926<br/>node scripts/ontology-condense-scheduler.mjs --loop, priority 234"]
    C1 --> A4["dream-engine flake.nix:2324<br/>dream-engine --loop --agentbox-toml /etc/agentbox.toml, priority 230"]
    C2 --> B1["podcast-cron flake.nix:2435<br/>supercronic -split-logs skills/podcast-knowledge-ingest/crontab, priority 250"]
    C2 --> B2["forum-backup-cron flake.nix:2455<br/>supercronic -split-logs workspace/dreamlab-ai-website/scripts/backup/crontab, priority 250"]
    A1 -.-> M1["memory sweep internals belong to AB-21"]
    A2 -.-> M1
    A3 -.-> M2["ontology condensation internals belong to AB-25"]
    A4 -.-> M3["dream-engine internals belong to AB-23"]
    B1 -.-> N1["log caps stdout and stderr maxbytes 5MB — flake.nix:2445-2446"]
    B2 -.-> N2["crontab and script live in the MOUNTED website repo, so schedule and behaviour<br/>are editable WITHOUT an image rebuild — only the supervisor stanza is baked (flake.nix:2450-2451)"]
    SUP -.-> ADHOC["NOT supervised — no ruflo daemon runs under supervisord and the runtime pins<br/>RUFLO_DAEMON_AI_WORKERS=0, so anything the reaper finds was started ad hoc<br/>inside a session (ruflo-daemon-gc.rs:8-10)"]
```

## AB-07.2 Self-gating loop daemons — why autostart is safe
```mermaid
stateDiagram-v2
    [*] --> Spawned
    Spawned --> SelfGate: process reads its own gate env from PID 1
    SelfGate --> ExitFast: gate off
    SelfGate --> Looping: gate on
    ExitFast --> Restarted: autorestart true
    Restarted --> SelfGate
    Looping --> Working: work available
    Working --> Sleeping: batch done
    Sleeping --> Looping
    Looping --> Crashed: unhandled error
    Crashed --> Restarted
    Restarted --> [*]: supervisord gives up after the default retry budget

    note right of SelfGate
        ontology-condense-scheduler self-gates on
        ONTOLOGY_CONDENSE_SCHEDULE and _ENABLED
        both baked into imageEnv and inherited
        from PID 1 - flake.nix:1853-1854
    end note
    note right of ExitFast
        exits fast when off, so autostart=true costs
        nothing when the feature is gated off
        flake.nix:1853-1855
    end note
    note left of Spawned
        startsecs=0 on every CLASS A loop
        flake.nix:1815, 1827, 1845
        so supervisord does not wait to call it up
    end note
    note right of Restarted
        DOC-DRIFT no CLASS A program sets startretries
        all rely on the supervisord default
    end note
```

## AB-07.3 ADR-2032 — the launcher allowlist that decides what is a daemon
```mermaid
flowchart TD
    ARGV["argv elements from /proc/pid/cmdline<br/>NEVER a joined command string"] --> P["is_ruflo_daemon_argv(args)<br/>process_identity.rs:327"]
    P --> B0{"args.first() present?<br/>process_identity.rs:328"}
    B0 -->|no| NO["false — fail closed"]
    B0 -->|yes| BN["basename(program)<br/>process_identity.rs:311"]
    BN --> M1{"basename is ruflo or claude-flow?<br/>process_identity.rs:332"}
    M1 -->|yes| TAIL["daemon_args = args[1..]"]
    M1 -->|no| M2{"basename is node or nodejs?<br/>process_identity.rs:333"}
    M2 -->|no| NO
    M2 -->|yes| SC{"args.get(1) script present?<br/>process_identity.rs:334"}
    SC -->|no| NO
    SC -->|yes| NL{"script basename is ruflo or claude-flow?<br/>process_identity.rs:337"}
    NL -->|yes| TAIL2["daemon_args = args[2..]"]
    NL -->|no| PS{"script basename is cli.js AND a path component<br/>is ruflo, claude-flow or @claude-flow?<br/>process_identity.rs:338-344"}
    PS -->|no| NO
    PS -->|yes| TAIL2
    TAIL --> FIN{"daemon_args starts_with the SEPARATE<br/>elements daemon then start?<br/>process_identity.rs:352"}
    TAIL2 --> FIN
    FIN -->|yes| YES["true — daemon-shaped"]
    FIN -->|no| NO
    ARGV --> H["is_hermes_daemon_argv(args)<br/>process_identity.rs:364"]
    H --> H1{"basename(program) == hermes-scheduler?<br/>process_identity.rs:368"}
    H1 -->|no| NO
    H1 -->|yes| H2{"args.get(1) == run-loop?<br/>process_identity.rs:371"}
    H2 -->|yes| YES
    H2 -->|no| NO
    YES -.-> INV["INVARIANT ADR-2032 — a shell whose command STRING contains the words,<br/>or an unrecognised wrapper, does NOT qualify. Unknown launchers fail closed<br/>process_identity.rs:318-320 and :359-360"]
    YES -.-> NOTID["Recognising a command SHAPE is not identity —<br/>it is defence in depth. A stop path must still verify<br/>the recorded tuple (process_identity.rs:9-13 and :362-363)"]
```

## AB-07.4 ProcessIdentity — the (pid, argv, starttime) tuple and its four verdicts
```mermaid
stateDiagram-v2
    [*] --> Launched
    Launched --> Captured: capture records pid, argv and starttime
    Captured --> Persisted: identity saved alongside the PID
    Persisted --> Verifying: caller is about to signal
    Verifying --> Match: same argv and same start time
    Verifying --> Mismatch: PID is live but is another process
    Verifying --> NoSuchProcess: PID is gone
    Verifying --> Inaccessible: proc could not be read at all
    Match --> Signalled: the ONLY verdict that permits a signal
    Mismatch --> Refused: refuse and report reuse
    NoSuchProcess --> TreatedStopped: refuse and treat as already stopped
    Inaccessible --> Refused: refuse and report, never assume a match
    Signalled --> [*]
    Refused --> [*]
    TreatedStopped --> [*]

    note right of Captured
        starttime is field 22 of /proc/pid/stat
        clock ticks since boot
        process_identity.rs:15-18
    end note
    note right of Match
        the kernel never reissues a pid plus starttime
        pair for a different process, so the pair is an
        identity not a guess - process_identity.rs:17-18
    end note
    note left of Verifying
        verify re-reads /proc IMMEDIATELY before the
        caller signals, never on an earlier verdict
        process_identity.rs:21-23
    end note
    note right of Inaccessible
        ProcError Inaccessible is never evidence about
        the target process, only about our own access
        process_identity.rs:47-49
    end note
    note left of Persisted
        /proc is reached through the ProcSource trait so
        tests inject a denied or absent procfs
        process_identity.rs:35-37 and :68-73
    end note
```

## AB-07.5 ruflo-daemon-gc — registry-first discovery with a PID-reuse guard
```mermaid
sequenceDiagram
    autonumber
    participant OP as operator or scheduled sweep
    participant GC as ruflo-daemon-gc main<br/>bin/ruflo-daemon-gc.rs:109
    participant REG as registry_daemons<br/>bin/ruflo-daemon-gc.rs:51
    participant SW as procs.sweep<br/>procs.rs:52
    participant PR as procs.confirm_daemon<br/>procs.rs:77
    participant K as rustix kill_process

    OP->>GC: ruflo-daemon-gc [--kill] [--ttl 43200] [--json]
    GC->>REG: read ~/.claude-flow registry files
    REG->>REG: ai-jobs.json, workspace-leases.json, repo-supervisors.json (:19-23)
    REG->>REG: registry_pid(pid) rejects zero, negative casts and truncation (:71 and :91)
    REG-->>GC: BTreeMap pid to Discovered source registry
    GC->>SW: procs.sweep() process-table fallback
    SW->>SW: sysinfo snapshot, filter by is_ruflo_daemon_argv (procs.rs:13 and :59)
    SW-->>GC: DaemonProc pid, cmdline, run_time_secs, workspace
    GC->>GC: or_insert entries not already in the registry, source ps (:116-119)
    loop for each discovered pid (:126)
        GC->>PR: confirm_daemon(pid)
        alt Some(false) — PID now belongs to something else
            PR-->>GC: skip this entry entirely (:129)
        else Some(true)
            PR-->>GC: confirmed = true
        else None — unconfirmable
            PR-->>GC: confirmed = false
        end
        GC->>GC: workspace_gone = workspace named, non-empty and not a directory (:134-135)
        GC->>GC: stale = workspace_gone OR run_time_secs > ttl (:136)
        GC->>GC: row with pid, workspace, source, age_s, confirmed, workspace_gone, stale (:146-154)
    end
    alt --kill passed (:158)
        loop stale but unconfirmed (:159)
            GC-->>OP: refuse pid — cmdline unconfirmable (pid reuse guard) (:160)
        end
        loop stale and confirmed (:162)
            GC->>PR: RE-PROBE immediately before signalling — the guard, not a formality (:163-164)
            alt no longer confirmed
                GC-->>OP: refuse pid (:165)
            else still confirmed
                GC->>K: kill_process(Pid::from_raw(pid), Signal::Term) (:171)
                alt Ok
                    K-->>GC: killed.push(pid) (:172)
                else Err
                    K-->>GC: kill failed, reported not swallowed (:173)
                end
            end
        end
    end
    Note over GC: default TTL 43200 s = 12 h (bin/ruflo-daemon-gc.rs:35-36)
    Note over GC,SW: discovery in procs.rs is READ-ONLY — nothing in that module signals anything (procs.rs:11)
    Note over SW: workspace_argv reads --workspace as ONE argument, preserving spaces and flag-like text in paths, and supports both spellings (procs.rs:27-45)
    Note over GC,K: INVARIANT — unconfirmable PIDs are REFUSED, never killed (ruflo-daemon-gc.rs:5-6)
```

## AB-07.6 Hermes daemon_stop — identity-verified signalling and observed exit
```mermaid
sequenceDiagram
    autonumber
    participant C as caller
    participant S as daemon_stop_with<br/>hermes/mod.rs:543
    participant P as probe_with<br/>hermes/mod.rs DaemonProbe :240
    participant ST as Store load_identity<br/>hermes/jobs.rs:135
    participant V as process_identity.verify<br/>process_identity.rs
    participant SIG as signal fn

    C->>S: daemon_stop_with(store, source, signal, grace)
    S->>P: probe_with(store, source)
    alt NoPidFile
        P-->>S: StopOutcome NotRunning pid None (:550)
    else Gone
        P-->>S: clear_daemon_record then NotRunning (:551-555)
    else Unverifiable
        P-->>S: RefusedUnverifiable (:556-558)
    else Mismatch
        P-->>S: RefusedMismatch (:559-561)
    else Inaccessible
        P-->>S: RefusedInaccessible (:562-564)
    else Running
        P-->>S: pid (:565)
    end
    S->>ST: load_identity()
    alt record disappeared between probe and signal
        ST-->>S: RefusedUnverifiable — the identity record disappeared (:569-574)
    end
    S->>V: verify(source, recorded) — re-check IMMEDIATELY before signalling (:568 and :575)
    alt Match
        V-->>S: proceed (:576)
    else NoSuchProcess
        V-->>S: clear_daemon_record then NotRunning (:577-580)
    else Mismatch
        V-->>S: RefusedMismatch (:581-583)
    else Inaccessible
        V-->>S: RefusedInaccessible (:584-586)
    end
    S->>SIG: signal(pid) (:589)
    alt signal errored
        SIG-->>S: StopOutcome SignalFailed (:590)
    end
    loop poll until grace elapses (:595-615)
        S->>V: verify(source, recorded)
        alt NoSuchProcess or Mismatch
            V-->>S: clear_daemon_record then Exited with waited_ms (:599-605)
        else Inaccessible or Match
            alt grace elapsed
                V-->>S: Signalled with waited_ms — delivered but exit NOT observed (:607-611)
            else
                S->>S: sleep STOP_POLL (:613)
            end
        end
    end
    Note over S: StopOutcome variants NotRunning, Exited, Signalled, RefusedUnverifiable, RefusedMismatch, RefusedInaccessible, SignalFailed — hermes/mod.rs:437-455
    Note over S,SIG: kill(2) returning Ok proves only that the signal was DELIVERED, not that the process<br/>exited — the two are reported as different outcomes (hermes/mod.rs:434-435)
    Note over C,V: DOC-DRIFT — BASELINE-container "Process lifecycle qualification 2026-09-04" says<br/>ADR-2032 is partial because Hermes Stop uses PID EXISTENCE ONLY. The code verifies<br/>the full recorded identity twice (hermes/mod.rs:575 and :596) and observes exit. The<br/>doc understates the implementation.
    Note over C,V: RESOLVED ADR-2039: BASELINE-container.md:231 marks this qualification<br/>resolved with evidence — identity verified at :575 and :596, delivery<br/>reported separately from confirmed exit at :434-435.
```

## AB-07.7 Cron runners — schedule outside the image
```mermaid
sequenceDiagram
    autonumber
    participant SUP as supervisord
    participant SC as supercronic<br/>flake.nix:2436 and :2456
    participant CT as crontab file
    participant J as job process
    participant L as /var/log split logs

    SUP->>SC: start podcast-cron priority 250, autostart, autorestart (flake.nix:2439-2442)
    SC->>CT: read skills/podcast-knowledge-ingest/crontab (flake.nix:2436)
    loop on each cron expression match
        SC->>J: run with PATH podcastIngestPkg, pythonRuntimeEnv, coreutils, nodejs_22, /usr/local/bin, /bin, /usr/bin (flake.nix:2438)
        J-->>SC: exit code
        SC->>L: -split-logs writes stdout and stderr separately
    end
    SUP->>SC: start forum-backup-cron priority 250 (flake.nix:2455)
    SC->>CT: read workspace/dreamlab-ai-website/scripts/backup/crontab (flake.nix:2456)
    loop nightly
        SC->>J: run with PATH coreutils, gnugrep, findutils, curl, jq, gzip (flake.nix:2458)
        alt CLOUDFLARE_API_TOKEN and ACCOUNT_ID absent
            J-->>SC: fails LOUD with exit 2 (flake.nix:2452)
        else credentials present
            J-->>SC: backup written to the NAS
        end
    end
    Note over SC,CT: both crontabs live OUTSIDE the image on mounted paths, so schedule and behaviour<br/>change without a rebuild — only the supervisor stanza is baked (flake.nix:2450-2451)
    Note over L: stdout_logfile_maxbytes and stderr_logfile_maxbytes are 5MB on both (flake.nix:2445-2446 and :2465-2466)
    Note over SUP,SC: environment HOME=/home/devuser and user=devuser on both — no cron job runs as root (flake.nix:2437 and :2457)
```

## AB-07.8 Backup and restore — volumes plus a crash-consistent PG dump
```mermaid
sequenceDiagram
    autonumber
    participant OP as operator
    participant B as cmd_backup<br/>agentbox.sh:270
    participant W as mktemp -d work dir
    participant V as docker volumes
    participant PG as ruvector-postgres
    participant T as output tarball

    OP->>B: ./agentbox.sh backup [--out PATH] [--include-secrets]
    B->>B: timestamp = date -u +%Y%m%dT%H%M%SZ (agentbox.sh:281)
    alt --out not given
        B->>B: out = SCRIPT_DIR/backups/agentbox-backup-TIMESTAMP.tgz (agentbox.sh:283-286)
    end
    B->>W: mktemp -d with trap rm -rf on EXIT (agentbox.sh:289-290)
    B->>V: _volume_tar agentbox-ruvector-data, always included (agentbox.sh:296-297)
    V-->>W: volumes/ruvector-data
    B->>PG: docker exec ruvector-postgres pg_isready -U ruvector -d ruvector (agentbox.sh:305)
    alt PG reachable
        B->>PG: pg_dump -U ruvector -Fc ruvector into work/ruvector-pg.dump (agentbox.sh:307)
        alt dump succeeded
            PG-->>W: ruvector-pg.dump, rv_pg_dumped = 1
        else dump failed
            B->>W: rm -f the partial dump (agentbox.sh:310)
        end
    else PG not running
        B-->>OP: WARNING ruvector-postgres not running — memory DB NOT included in this backup (agentbox.sh:314)
    end
    B->>T: write manifest with ruvector_data true and ruvector_pg_dump boolean (agentbox.sh:387-389)
    B-->>OP: tarball at out
    OP->>B: ./agentbox.sh restore (cmd_restore agentbox.sh:423)
    B->>V: restore ruvector-data when work/volumes/ruvector-data is present (agentbox.sh:487-490)
    Note over B,PG: a raw volume tar of a LIVE PG datadir is not crash-consistent, so the backup carries pg_dump -Fc instead (agentbox.sh:299-303)
    Note over B: the memory DB, 2M+ rows, was previously absent from backups entirely — PRD-018 Phase 0 closes gap 12 (agentbox.sh:299)
    Note over T: on-disk layout backups/ruvector-sidecar holds state.json, timestamped .copy.gz<br/>snapshots from the gated ops, and recall-runs/<utc>.json from the recall harness —<br/>see AB-05.6
```

## AB-07.9 Gated starts — networking, desktop and toolchain daemons
```mermaid
flowchart TD
    G["flake.nix lib.optionalString gates — see AB-01.1"] --> N["tailscaled flake.nix:2255<br/>tailscale-up flake.nix:2265"]
    G --> D["desktop stack gated on desktop.enabled"]
    G --> T["toolchain surfaces"]
    D --> D1["xvnc flake.nix:2082"]
    D --> D2["x11vnc flake.nix:2071"]
    D --> D3["wayvnc flake.nix:2039"]
    D --> D4["xorg-nvidia flake.nix:2050"]
    D --> D5["hyprland flake.nix:2014"]
    D --> D6["xwayland-session flake.nix:2028"]
    D --> D7["i3wm flake.nix:2060 and :2092 — TWO blocks,<br/>mutually exclusive Nix branches, see AB-02.7"]
    T --> T1["jupyter-lab flake.nix:1939<br/>gate skills.data_science.jupyter, binds 0.0.0.0:8888"]
    T --> T2["code-server flake.nix:2278<br/>gate toolchains.code_server, priority 50"]
    T --> T3["comfyui-builtin flake.nix:2302<br/>gate skills.media.comfyui_builtin"]
    T --> T4["qgis-mcp flake.nix:1843, blender-mcp flake.nix:1868,<br/>imagemagick-mcp flake.nix:2194"]
    T2 -.-> CSR["RESOLVED ADR-2040 (2026-09-05) — the old finding was that code-server ran<br/>--bind-addr 0.0.0.0:8080 --auth none ([program:code-server]), unauthenticated<br/>to any sibling container on visionclaw_network (BASELINE's flake.nix:1927<br/>citation for this was itself already stale). It now runs --auth password<br/>with the credential minted at boot into<br/>/home/devuser/.local/share/code-server/config.yaml (0600). See AB-06.7."]
    T1 -.-> JR["RESOLVED ADR-2040 (2026-09-05) — the old finding was that jupyter also bound<br/>0.0.0.0 with an empty --IdentityProvider.token=, relying on the loopback-only<br/>host publish for its boundary. The flag is gone; JUPYTER_TOKEN is now minted<br/>at boot into a 0600 devuser file and exported into PID 1's environment before<br/>supervisord starts ([program:jupyter-lab]:1939). See AB-06.7."]
    N -.-> ND["nostr-gateway flake.nix:1896 is the INBOUND half of the session mirror —<br/>AGENTBOX_PRIVKEY_HEX is injected by the entrypoint launcher and inherited,<br/>never written into the generated supervisor text. Off switch AGENTBOX_NOSTR_GATEWAY=0<br/>(flake.nix:1890-1895). See AB-08 for the outbound mirror hook"]
    G -.-> INV["INVARIANT — adding a gate means gating BOTH the Nix package set AND the<br/>supervisor block, plus a system-manifest catalogue entry with an honest<br/>apply class. See AB-05.9"]
```
