{
  "repo": "DreamLab-AI/VisionFlow",
  "cron": "0 3 * * *",
  "slots": [
    {
      "deep": "content-integrity",
      "scan": ["internal-links", "asset-refs"]
    },
    {
      "deep": "build-pipeline",
      "scan": ["static-build", "cname-domain"]
    },
    {
      "deep": "webgl-mesh",
      "scan": ["shader-source", "canvas-fallback"]
    },
    {
      "deep": "seo-and-meta",
      "scan": ["meta-tags", "structured-data"]
    }
  ],
  "bonusModuli": {
    "30": "copy-and-messaging-review",
    "60": "asset-weight-budget"
  },
  "controlPlaneProbes": [
    "node --version",
    "ls website/static website/dist 2>&1 | head -20 || true"
  ],
  "buildStep": {
    "cmd": "cd website && ./build.sh 2>&1 | tail -8",
    "degradeOnWasmFailure": false
  },
  "evaluatorEntrypoints": {
    "build-check": "cd website/dist 2>/dev/null && test -f index.html && echo \"pages: $(find . -name '*.html' | wc -l)  bytes: $(du -sb . | cut -f1)\" && echo BUILD-OK || echo BUILD-FAIL",
    "links": "bash scripts/dream-link-check.sh 2>&1 | tail -6"
  },
  "competitors": [],
  "adrConvention": "4-digit",
  "extraDisciplines": [
    "static-site-only: this is a hand-written HTML/CSS/JS site with a self-contained WebGL2 mesh — no bundler, no WASM, no compile. Hypotheses must respect that; do not propose introducing a build framework.",
    "browser-checks-out-of-annexe: accessibility, performance (Lighthouse), and playwright visual tests need the browsercontainer sidecar, which the HP annexe does NOT have. Never invent a browser evaluator here — record such findings as 'HANDOFF (browser):' notes for a sidecar-equipped run (npm run test:a11y / test:perf locally).",
    "content-is-the-product: copy, messaging, and asset integrity are the surfaces that matter for a marketing site — weight them above code-structure concerns."
  ],
  "ledgerPath": "docs/dream-cycle/LEDGER.md",
  "branchPrefix": "dream/",
  "labels": ["dream-cycle", "marketing-site"],
  "autoMerge": false
}
