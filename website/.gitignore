target/
dist/

# Build outputs, regenerated by build.sh on every run and uploaded as a CI
# artefact rather than committed: the receipt describes one specific build
# (timestamps, the dist tree digest, the published revision), so committing it
# would create diff noise and invite reading a stale receipt as current.
build-receipt.json
.staging-report.json
